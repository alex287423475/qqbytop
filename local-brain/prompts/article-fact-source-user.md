请根据提供的关键词、意图、分类、内容模式和内链要求，输出一篇“核心事实源”级别的 Markdown 文章。

这不是普通 SEO 博客。目标是让页面具备被搜索引擎、AI Overview、Perplexity、ChatGPT 等系统引用的潜力。

硬性要求：
- 输出完整 Markdown 文件，必须包含 frontmatter。
- frontmatter 必须包含：title、slug、description、category、date、locale、keywords、faq。
- 正文必须包含 H1、至少 5 个 H2、至少 2 个 Markdown 表格、至少 5 个 FAQ、至少 1 个 CTA。
- 中文正文建议不少于 2200 字；如果信息不足，也必须优先增加判断框架、风险矩阵、对比表格，而不是堆砌空话。
- 不要写成“注意事项大全”。必须围绕一个具体高价值场景，给出判断标准、证据链、失败原因、修正示例和可执行流程。
- 必须同时适合企业客户和个人客户阅读，但不能把全部读者都预设为企业采购。
- 公司名称统一写作“北京全球博译翻译公司”，不要把“QQBY”单独作为关键词或正文主表达。

深度结构要求：
1. 开篇必须有“核心结论”区，用 3-5 条短句直接回答搜索意图。
2. 必须包含“适用场景 / 不适用场景”对比表，帮助读者判断自己是否需要专业翻译服务。
3. 必须包含“常见错误 -> 审核方/接收方可能如何理解 -> 建议修正”的 Before/After 表格。
4. 必须包含“证据或材料清单”，说明客户提交哪些材料能提升报价和交付准确性。
5. 必须包含“北京全球博译翻译公司的处理标准”一节，写具体流程，不写营销口号。
6. 必须包含“哪些情况需要人工确认”一节，把不能自动判断的高风险点列出来。
7. 必须包含站内内链，格式为 `[链接文字](/zh/services/xxx)` 或 `[链接文字](/zh/quote)`。

写作边界：
- 不要编造具体成功率、客户名称、法律条款编号、政府政策日期。
- 如果需要举例，使用“脱敏示例”“常见情形”“项目中经常遇到的情况”等表述。
- 不要承诺“保证通过”“100%准确”“全网最低价”等不可控结果。
- 不要输出“AI质检后修订说明”“本文由AI生成”等内部流程说明。

输出只包含最终 Markdown 文章，不要附加解释。
